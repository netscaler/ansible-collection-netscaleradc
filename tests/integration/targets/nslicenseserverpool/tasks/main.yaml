---
- name: NSLICENSESERVERPOOL | GET
  delegate_to: localhost
  register: result
  check_mode: false
  tags: test
  netscaler.adc.nslicenseserverpool:
    nsip: "{{ nsip }}"
    nitro_user: "{{ nitro_user }}"
    nitro_pass: "{{ nitro_pass }}"
    nitro_protocol: "{{ nitro_protocol }}"
    validate_certs: "{{ validate_certs }}"
    save_config: "{{ save_config }}"
    state: present

- name: Assert | NSLICENSESERVERPOOL | GET
  tags: test
  ansible.builtin.assert:
    that:
      - "result.failed==false"
