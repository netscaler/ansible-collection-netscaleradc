---
- name: NSHTTPPROFILE | ADD | --check
  delegate_to: localhost
  register: result
  check_mode: true
  tags: test
  netscaler.adc.nshttpprofile:
    nsip: "{{ nsip }}"
    nitro_user: "{{ nitro_user }}"
    nitro_pass: "{{ nitro_pass }}"
    nitro_protocol: "{{ nitro_protocol }}"
    validate_certs: "{{ validate_certs }}"
    save_config: "{{ save_config }}"
    state: present
    name: ansible_test_httpprofile
    http2: ENABLED
    http2smallwndtimeout: 30
- name: Assert | NSHTTPPROFILE | ADD | --check
  tags: test
  ansible.builtin.assert:
    that:
      - "result.failed==false"
      - "result.changed==true"
- name: NSHTTPPROFILE | ADD
  delegate_to: localhost
  register: result
  check_mode: false
  tags: test
  netscaler.adc.nshttpprofile:
    nsip: "{{ nsip }}"
    nitro_user: "{{ nitro_user }}"
    nitro_pass: "{{ nitro_pass }}"
    nitro_protocol: "{{ nitro_protocol }}"
    validate_certs: "{{ validate_certs }}"
    save_config: "{{ save_config }}"
    state: present
    name: ansible_test_httpprofile
    http2: ENABLED
    http2smallwndtimeout: 30
- name: Assert | NSHTTPPROFILE | ADD
  tags: test
  ansible.builtin.assert:
    that:
      - "result.failed==false"
      - "result.changed==true"
- name: NSHTTPPROFILE | ADD | idempotent
  delegate_to: localhost
  register: result
  check_mode: false
  tags: test
  netscaler.adc.nshttpprofile:
    nsip: "{{ nsip }}"
    nitro_user: "{{ nitro_user }}"
    nitro_pass: "{{ nitro_pass }}"
    nitro_protocol: "{{ nitro_protocol }}"
    validate_certs: "{{ validate_certs }}"
    save_config: "{{ save_config }}"
    state: present
    name: ansible_test_httpprofile
    http2: ENABLED
    http2smallwndtimeout: 30
- name: Assert | NSHTTPPROFILE | ADD | idempotent
  tags: test
  ansible.builtin.assert:
    that:
      - "result.failed==false"
      - "result.changed==false"
- name: NSHTTPPROFILE | UPDATE http2smallwndtimeout
  delegate_to: localhost
  register: result
  check_mode: false
  tags: test
  netscaler.adc.nshttpprofile:
    nsip: "{{ nsip }}"
    nitro_user: "{{ nitro_user }}"
    nitro_pass: "{{ nitro_pass }}"
    nitro_protocol: "{{ nitro_protocol }}"
    validate_certs: "{{ validate_certs }}"
    save_config: "{{ save_config }}"
    state: present
    name: ansible_test_httpprofile
    http2: ENABLED
    http2smallwndtimeout: 60
- name: Assert | NSHTTPPROFILE | UPDATE http2smallwndtimeout
  tags: test
  ansible.builtin.assert:
    that:
      - "result.failed==false"
      - "result.changed==true"
- name: NSHTTPPROFILE | UPDATE http2smallwndtimeout | idempotent
  delegate_to: localhost
  register: result
  check_mode: false
  tags: test
  netscaler.adc.nshttpprofile:
    nsip: "{{ nsip }}"
    nitro_user: "{{ nitro_user }}"
    nitro_pass: "{{ nitro_pass }}"
    nitro_protocol: "{{ nitro_protocol }}"
    validate_certs: "{{ validate_certs }}"
    save_config: "{{ save_config }}"
    state: present
    name: ansible_test_httpprofile
    http2: ENABLED
    http2smallwndtimeout: 60
- name: Assert | NSHTTPPROFILE | UPDATE http2smallwndtimeout | idempotent
  tags: test
  ansible.builtin.assert:
    that:
      - "result.failed==false"
      - "result.changed==false"
- name: NSHTTPPROFILE | DELETE | --check
  delegate_to: localhost
  register: result
  check_mode: true
  tags: test
  netscaler.adc.nshttpprofile:
    nsip: "{{ nsip }}"
    nitro_user: "{{ nitro_user }}"
    nitro_pass: "{{ nitro_pass }}"
    nitro_protocol: "{{ nitro_protocol }}"
    validate_certs: "{{ validate_certs }}"
    save_config: "{{ save_config }}"
    state: absent
    name: ansible_test_httpprofile
- name: Assert | NSHTTPPROFILE | DELETE | --check
  tags: test
  ansible.builtin.assert:
    that:
      - "result.failed==false"
      - "result.changed==true"
- name: NSHTTPPROFILE | DELETE
  delegate_to: localhost
  register: result
  check_mode: false
  tags: test
  netscaler.adc.nshttpprofile:
    nsip: "{{ nsip }}"
    nitro_user: "{{ nitro_user }}"
    nitro_pass: "{{ nitro_pass }}"
    nitro_protocol: "{{ nitro_protocol }}"
    validate_certs: "{{ validate_certs }}"
    save_config: "{{ save_config }}"
    state: absent
    name: ansible_test_httpprofile
- name: Assert | NSHTTPPROFILE | DELETE
  tags: test
  ansible.builtin.assert:
    that:
      - "result.failed==false"
      - "result.changed==true"
- name: NSHTTPPROFILE | DELETE | idempotent
  delegate_to: localhost
  register: result
  check_mode: false
  tags: test
  netscaler.adc.nshttpprofile:
    nsip: "{{ nsip }}"
    nitro_user: "{{ nitro_user }}"
    nitro_pass: "{{ nitro_pass }}"
    nitro_protocol: "{{ nitro_protocol }}"
    validate_certs: "{{ validate_certs }}"
    save_config: "{{ save_config }}"
    state: absent
    name: ansible_test_httpprofile
- name: Assert | NSHTTPPROFILE | DELETE | idempotent
  tags: test
  ansible.builtin.assert:
    that:
      - "result.failed==false"
      - "result.changed==false"
