---

- name: Remove ssl certkey
  delegate_to: localhost
  register: result
  check_mode: "{{ check_mode }}"
  citrix_adc_ssl_certkey:
    nitro_user: "{{nitro_user}}"
    nitro_pass: "{{nitro_pass}}"
    nsip: "{{nsip}}"
    validate_certs: no

    state: absent
    certkey: certificate_1
