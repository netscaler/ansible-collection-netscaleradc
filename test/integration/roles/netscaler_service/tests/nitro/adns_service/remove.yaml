---

- name: Remove adns service
  delegate_to: localhost
  register: result
  netscaler_service:

    nitro_user: "{{nitro_user}}"
    nitro_pass: "{{nitro_pass}}"
    nsip: "{{nsip}}"

    operation: absent
    name: service-adns
