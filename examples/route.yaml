- name: Sample Playbook
  hosts: demo_netscalers
  gather_facts: false
  tasks:
    - name: Sample Task | route
      delegate_to: localhost
      netscaler.adc.route:
        state: present
        network: 0.0.0.0
        netmask: 0.0.0.0
        gateway: 10.10.10.1
