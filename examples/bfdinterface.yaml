---
- name: Configure bfdinterface
  hosts: localhost
  tasks:
    - name: Create bfdinterface
      delegate_to: localhost
      netscaler.adc.bfdinterface:
        state: present
        name: vlan0
        passive: true
        interval: 752
        minrx: 501
        multiplier: 3
