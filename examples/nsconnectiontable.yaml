---
- name: Sample nsconnectiontable playbook
  hosts: demo_netscalers
  gather_facts: false
  tasks:
    - name: Configure nsconnectiontable
      delegate_to: localhost
      netscaler.adc.nsconnectiontable:
        state: present
        filterexpression: CONNECTION.SRCIP.EQ(10.102.233.126)
        listen: true
