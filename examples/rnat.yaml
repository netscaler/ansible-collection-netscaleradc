---
- name: Sample rnat playbook
  hosts: demo_netscalers
  gather_facts: false
  tasks:
    - name: Configure rnat
      delegate_to: localhost
      netscaler.adc.rnat:
        state: present
        name: 1.2.2.0
        connfailover: ENABLED
